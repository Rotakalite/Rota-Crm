import React from 'react';
import ReactDOM from 'react-dom/client';
import EmailManagementTest from './EmailTest';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <EmailManagementTest />
  </React.StrictMode>
);